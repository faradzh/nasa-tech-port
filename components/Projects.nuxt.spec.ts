import { describe, it, expect, vi } from "vitest";

describe("Projects", () => {
  it("should render main page", () => {
    // I wish I could write more tests here but really running out if time...
  });
});
